[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "langchain-openai>=1.5.1",
    "langsmith>=0.11.0",
    "python-dotenv>=1.2.3",
]
